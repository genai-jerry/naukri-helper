# Copy to .env and fill in. .env is gitignored.

# Anthropic API key — used by research, rank, tailor, verify stages.
ANTHROPIC_API_KEY=

# Path to your structured master resume (YAML). Created via `naukri-tool bootstrap`.
RESUME_MASTER_PATH=resumes/master.yaml
